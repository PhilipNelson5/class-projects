[X] Renders a skybox

[X] Renders one of the Stanford ply models with:
  [X] Per-fragment ambient
  [X] Per-fragment diffuse

[X] Renders one of the Stanford ply models with:
  [X] Per-fragment ambient
  [X] Per-fragment diffuse
  [X] Per-fragment specular

[X] Renders one of the Stanford ply models with:
  [X] Per-fragment reflection mapping; 100% reflection

[X] Renders one of the Stanford ply models with:
  [X] 80% per-fragment diffuse and specular
  [X] 20% per-fragment reflection mapping

[X] Uses perspective projection.

[ ] Provides an "interesting" animation/demonstration of the rendering techniques
  [X] Shininess slider
  [X] Refractive index slider
  [X] reflection / refraction toggle
